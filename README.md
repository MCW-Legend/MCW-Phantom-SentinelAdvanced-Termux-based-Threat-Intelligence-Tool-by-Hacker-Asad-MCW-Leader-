## ⚙️ Installation & Setup (Termux)

```bash
# STEP 1: Clone the tool from GitHub
git clone https://github.com/MCW-Legend/MCW-Phantom-SentinelAdvanced-Termux-based-Threat-Intelligence-Tool-by-Hacker-Asad-MCW-Leader-.git

# STEP 2: Go into the directory
cd MCW-Phantom-Sentinel

# STEP 3: Give execute permission to install script
chmod +x install.sh

# STEP 4: Run the installer to setup everything automatically
bash install.sh
```
